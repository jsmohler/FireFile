/*
    Attributes
    users (list of users)
    directories (directory)
    files (File)
    name (String)
    projectAdvisor (User)
*/

function Group(users, directories, files, name, projectAdvisor)
{
    this.users = users;
    this.directories = directory;
    this.files = files;
    this.name = name;
    this.projectAdvisor = projectAdvisor;

    //OBJECT FUNCTIONS
    //This will get the directory of the group, which is the
    //directory's filepath on the server
    this.getDirectory = function(dirName)
    {
        //for(int i = 0; i < directories.;)
    }
}