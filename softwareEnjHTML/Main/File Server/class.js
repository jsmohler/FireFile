import {Group} from 'group';
/*
    Attributes
    courseCoordinator (User)
    groups: (Group)
        groups is an array of type Group
*/

function Class(courseCoordinator, groups)
{
    this.courseCoordinator = courseCoordinator;
    this.groups = groups;

    //Function to add a new group to Class
    this.addGroup = function(groupName)
    {
        //creates a new group with specified attributes
        var newGroup = new Group(users, directories, files, name, projectAdvisor)
        
    }
}

